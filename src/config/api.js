// API Configuration - Easy to swap domains
const API_CONFIG = {
  // Change this URL when you get your own domain
  BASE_URL: 'https://westernkits.com/api/v1',
  
  ENDPOINTS: {
    LOGIN: '/login',
    REGISTER: '/register',
    LOGOUT: '/logout',
    // Add more endpoints as needed
  }
};

export default API_CONFIG;